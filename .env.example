# 环境变量配置模板
#
# 认证已启用：除 /login 外的页面、业务 API 与 WebSocket 都必须先登录。
# 页面、WebSocket(/ws)、图片清单接口(/api/*) 仍由 server/index.js 一个进程、
# 一个端口(默认9999)提供，前端固定连同源地址，所以不管是 localhost、局域网IP
# 还是公网域名都自动正确，不需要告诉前端"WebSocket在哪"。

# ============ 多账户认证（必填） ============
# 会话签名密钥：至少 32 字节，推荐使用 `openssl rand -base64 48` 生成。
BOX_SESSION_SECRET=replace-with-a-unique-random-secret-at-least-32-bytes
# 可选：会话 cookie 的 Secure 标志。默认自动判断——请求走 HTTPS 才加，
# HTTP（局域网/内网 http://IP）不加，否则浏览器拒收 cookie 导致登录失效。
# 特殊场景（HTTPS 由反向代理终结且未转发 X-Forwarded-Proto）可显式设为 true。
# BOX_COOKIE_SECURE=true
# 可选：登录会话时长（秒），默认 43200（12 小时）。
# BOX_SESSION_TTL_SECONDS=43200
# 可选：用户配置文件路径，默认 data/auth-users.json。
# BOX_AUTH_USERS_FILE=/app/data/auth-users.json
#
# 多账户权限配置：
# 1. 复制 data/auth-users.example.json 为 data/auth-users.json（该文件已被 Git 忽略）；
# 2. 对每个账户运行 `node server/create-password-hash.js`，将输出填入 passwordHash；
# 3. permissions 可填 "*"（全部工具），或填工具 slug，例如 ["tarot-reading", "initiative-tracker"]。
#    initiative-tracker 权限自动包含其 /display 主屏幕。

# ============ 可选：改端口 ============
# PORT=9999

# ============ 可选：改图片目录 ============
# 默认开发读 public/image，生产读 out/image。
# IMAGE_DIR=/absolute/path/to/image
